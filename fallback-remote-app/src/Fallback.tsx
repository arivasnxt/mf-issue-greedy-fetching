
const Fallback = () => {
    return (
        <div style={{ backgroundColor: "red" }}>
            <p>This is the fallback module because the previous request failed</p>
        </div>
    )
}

export default Fallback;
